[package]
name = "serde_alloc"
version = "0.1.0-alpha.2"
edition = "2024"
description = "Deserialize using serde with a custom allocator"
license = "MIT"

[dependencies]
serde_core = "1"

[dev-dependencies]
serde_json = "1"
